# Loaded when running: npm run dev:https (vite --mode https)
VITE_HTTPS=true
# For remote access: set your PC's LAN IP so the HTTPS cert is valid for https://YOUR_IP:3001
# VITE_HTTPS_HOST=192.168.50.200
