# Optional: backend URL when frontend and backend are on different hosts (e.g. multiple PCs).
# Set to your backend base URL (e.g. http://192.168.1.10:4000) so API and realtime socket connect there.
# VITE_API_URL=http://localhost:4000

# Optional: when backend runs on a different port on the same machine, set proxy target (restart Vite after change).
# API_PROXY_TARGET=http://localhost:5000
